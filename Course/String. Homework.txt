Возьмите любой стих, который вам нравится, каждую строку стиха добавьте в переменную
создайте переменную result и сложите все строки через знак + предыдущих строк в эту переменную
Выведите данные через print используя переменную result

НАПРИМЕР:

str1 = “bla-bla-bla”
str2 =  “bla-bla-bla”
result = str1 + str2
print(result)

Читаем в интернете и практикуем:
- как посмотреть длину строки
- как обрезать начало строки, конец строки
- доступ по индексу